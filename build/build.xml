<?xml version="1.0" encoding="utf-8"?>
<!--
  This file is part of the YoudsFramework package.
  Copyright (c) 2005-2011 the YoudsFramework Project.
  Based on the Mojavi3 MVC Framework, Copyright (c) 2003-2005 Sean Kerr.

  For the full copyright and license information, please view the LICENSE file
  that was distributed with this source code. You can also view the LICENSE
  file online at http://www.youds.com/LICENSE.txt
    vi: set noexpandtab:
    Local Variables:
    indent-tabs-mode: t
    End:
  -->

<!--
  $Id$
  -->
<project name="YoudsFramework" basedir="." default="status">

	<!-- Dear Phing: Go fuck yourself. You fucking worthless piece of shit. -->
	<property userProperty="true" name="property-specifier" value="$" override="true" />

	<!-- Load tasks relative to the build system. -->
	<taskdef name="youds.input" classname="org.youds.build.tasks.YoudsFrameworkInputTask" classpath="phing" />

	<taskdef name="youds.import" classname="org.youds.build.tasks.YoudsFrameworkImportTask" classpath="phing" />

	<taskdef name="youds.transform-path" classname="org.youds.build.tasks.YoudsFrameworkTransformpathTask" classpath="phing" />
	<taskdef name="youds.transform-string-to-identifier" classname="org.youds.build.tasks.YoudsFrameworkTransformstringtoidentifierTask" classpath="phing" />
	<taskdef name="youds.transform-namespace-string-to-identifier" classname="org.youds.build.tasks.YoudsFrameworkTransformnamespacestringtoidentifierTask" classpath="phing" />
	<taskdef name="youds.transform-namespace-string-to-path" classname="org.youds.build.tasks.YoudsFrameworkTransformnamespacestringtopathTask" classpath="phing" />
	<taskdef name="youds.transform-string-to-youds-identifier" classname="org.youds.build.tasks.YoudsFrameworkTransformstringtoyoudsidentifierTask" classpath="phing" />
	<taskdef name="youds.transform-view-class-base" classname="org.youds.build.tasks.YoudsFrameworkTransformviewclassbaseTask" classpath="phing" />

	<taskdef name="youds.base-name" classname="org.youds.build.tasks.YoudsFrameworkBasenameTask" classpath="phing" />
	<taskdef name="youds.directory-name" classname="org.youds.build.tasks.YoudsFrameworkDirectorynameTask" classpath="phing" />

	<taskdef name="youds.available" classname="org.youds.build.tasks.YoudsFrameworkAvailableTask" classpath="phing" />

	<taskdef name="youds.write-properties" classname="org.youds.build.tasks.YoudsFrameworkWritepropertiesTask" classpath="phing" />

	<taskdef name="youds.target-listener" classname="org.youds.build.tasks.YoudsFrameworkTargetListenerTask" classpath="phing" />
	<taskdef name="youds.task-listener" classname="org.youds.build.tasks.YoudsFrameworkTaskListenerTask" classpath="phing" />
	<taskdef name="youds.message-listener" classname="org.youds.build.tasks.YoudsFrameworkMessageListenerTask" classpath="phing" />

	<taskdef name="youds.execute-target" classname="org.youds.build.tasks.YoudsFrameworkExecutetargetTask" classpath="phing" />

	<taskdef name="youds.iterate" classname="org.youds.build.tasks.YoudsFrameworkIterateTask" classpath="phing" />

	<taskdef name="youds.car" classname="org.youds.build.tasks.YoudsFrameworkCarTask" classpath="phing" />

	<taskdef name="youds.select-path" classname="org.youds.build.tasks.YoudsFrameworkSelectpathTask" classpath="phing" />

	<taskdef name="youds.generate-action-methods" classname="org.youds.build.tasks.YoudsFrameworkGenerateActionMethodsTask" classpath="phing" />
	<taskdef name="youds.generate-view-methods" classname="org.youds.build.tasks.YoudsFrameworkGenerateViewMethodsTask" classpath="phing" />
	<taskdef name="youds.generate-model-attributes" classname="org.youds.build.tasks.YoudsFrameworkGenerateModelAttributesTask" classpath="phing" />
	
	<!-- Load types relative to the build system. -->
	<typedef name="youds.object" classname="org.youds.build.types.YoudsFrameworkObjectType" classpath="phing" />

	<typedef name="youds.property" classname="org.youds.build.types.YoudsFrameworkPropertyType" classpath="phing" />
	<typedef name="youds.variable" classname="org.youds.build.types.YoudsFrameworkVariableType" classpath="phing" />

	<typedef name="youds.from" classname="org.youds.build.types.YoudsFrameworkFromType" classpath="phing" />

	<tstamp />

	<!-- Load user-defined build configuration. -->
	<if>
		<isset property="file.build-properties" />
		<then>
			<youds.transform-path property="file.build-properties" base="${application.startdir}" path="${file.build-properties}" />
			<property userProperty="true" file="${file.build-properties}" />
		</then>
	</if>

	<!-- Define YoudsFramework core directories. -->
	<property name="youds.directory.src" value="@PEAR_DIR@/youds" />
	<youds.transform-path property="youds.directory.src" base="${application.startdir}" path="${youds.directory.src}" />

	<property name="youds.directory.src.action" value="${youds.directory.src}/action" override="true" />
	<property name="youds.directory.src.config" value="${youds.directory.src}/config" override="true" />
	<property name="youds.directory.src.controller" value="${youds.directory.src}/controller" override="true" />
	<property name="youds.directory.src.core" value="${youds.directory.src}/core" override="true" />
	<property name="youds.directory.src.database" value="${youds.directory.src}/database" override="true" />
	<property name="youds.directory.src.date" value="${youds.directory.src}/date" override="true" />
	<property name="youds.directory.src.exception" value="${youds.directory.src}/exception" override="true" />
	<property name="youds.directory.src.filter" value="${youds.directory.src}/filter" override="true" />
	<property name="youds.directory.src.logging" value="${youds.directory.src}/logging" override="true" />
	<property name="youds.directory.src.model" value="${youds.directory.src}/model" override="true" />
	<property name="youds.directory.src.renderer" value="${youds.directory.src}/renderer" override="true" />
	<property name="youds.directory.src.request" value="${youds.directory.src}/request" override="true" />
	<property name="youds.directory.src.response" value="${youds.directory.src}/response" override="true" />
	<property name="youds.directory.src.routing" value="${youds.directory.src}/routing" override="true" />
	<property name="youds.directory.src.storage" value="${youds.directory.src}/storage" override="true" />
	<property name="youds.directory.src.testing" value="${youds.directory.src}/testing" override="true" />
	<property name="youds.directory.src.translation" value="${youds.directory.src}/translation" override="true" />
	<property name="youds.directory.src.user" value="${youds.directory.src}/user" override="true" />
	<property name="youds.directory.src.util" value="${youds.directory.src}/util" override="true" />
	<property name="youds.directory.src.validator" value="${youds.directory.src}/validator" override="true" />
	<property name="youds.directory.src.vendor" value="${youds.directory.src}/vender" override="true" />
	<property name="youds.directory.src.view" value="${youds.directory.src}/view" override="true" />

	<property name="youds.directory.src.build" value="${youds.directory.src}/build" override="true" />
	<property name="youds.directory.src.build.templates" value="${youds.directory.src.build}/templates" override="true" />
	<property name="youds.directory.src.build.tasks" value="${youds.directory.src.build}/tasks" override="true" />

	<!-- Define essential project directories. -->
	<property name="project.directory.app" value="app" />
	<property name="project.directory.app.config" value="${project.directory.app}/config" />
	<property name="project.directory.app.models" value="${project.directory.app}/models" />
	<property name="project.directory.app.modules" value="${project.directory.app}/modules" />
	<property name="project.directory.app.cache" value="${project.directory.app}/cache" />
	<property name="project.directory.app.lib" value="${project.directory.app}/lib" />
	<property name="project.directory.app.log" value="${project.directory.app}/log" />
	<property name="project.directory.app.templates" value="${project.directory.app}/templates" />
	<property name="project.directory.dev" value="dev" />
	<property name="project.directory.dev.pub" value="${project.directory.dev}/pub" />
	<property name="project.directory.libs" value="libs" />
	<property name="project.directory.pub" value="pub" />
	
	<!-- Ensure we can reach the YoudsFramework directory. -->
	<youds.available property="youds.available" file="${youds.directory.src}/youds.php" type="file" />
	<fail unless="youds.available" message="YoudsFramework installation not found in ${youds.directory.src}" />

	<!-- Load the remaining YoudsFramework-specific tasks. -->
	<taskdef name="youds.locate-project" classname="org.youds.build.tasks.YoudsFrameworkLocateprojectTask" classpath="phing" />
	<taskdef name="youds.check-project" classname="org.youds.build.tasks.YoudsFrameworkCheckprojectTask" classpath="phing" />

	<taskdef name="youds.locate-module" classname="org.youds.build.tasks.YoudsFrameworkLocatemoduleTask" classpath="phing" />
	<taskdef name="youds.check-module" classname="org.youds.build.tasks.YoudsFrameworkCheckmoduleTask" classpath="phing" />
	<taskdef name="youds.configure-module" classname="org.youds.build.tasks.YoudsFrameworkConfiguremoduleTask" classpath="phing" />

	<taskdef name="youds.list-modules" classname="org.youds.build.tasks.YoudsFrameworkListmodulesTask" classpath="phing" />
	<taskdef name="youds.display-modules" classname="org.youds.build.tasks.YoudsFrameworkDisplaymodulesTask" classpath="phing" />

	<taskdef name="youds.list-actions" classname="org.youds.build.tasks.YoudsFrameworkListactionsTask" classpath="phing" />
	<taskdef name="youds.display-actions" classname="org.youds.build.tasks.YoudsFrameworkDisplayactionsTask" classpath="phing" />

	<taskdef name="youds.read-configuration" classname="org.youds.build.tasks.YoudsFrameworkReadconfigurationTask" classpath="phing" />
	<taskdef name="youds.write-configuration" classname="org.youds.build.tasks.YoudsFrameworkWriteconfigurationTask" classpath="phing" />

	<taskdef name="youds.resolve-configuration" classname="org.youds.build.tasks.YoudsFrameworkResolveconfigurationTask" classpath="phing" />

	<!-- Load some basic configuration values. -->
	<youds.read-configuration property="youds.version" configurationValue="youds.version" />
	<youds.read-configuration property="youds.url" configurationValue="youds.url" />

	<target name="status" description="Displays information about the current environment">
		<!-- Print PHP status. -->
		<echo message="PHP:" />
		<echo message="  Version: ${php.version}" />
		<echo message="  Include path: ${php.classpath}" />
		<echo message="" />

		<!-- Print Phing status. -->
		<echo message="Phing:" />
		<echo message="  Version: ${phing.version}" />
		<echo message="" />

		<!-- Print YoudsFramework status. -->
		<echo message="YoudsFramework:" />
		<echo message="  Installation directory: ${youds.directory.src}" />
		<echo message="  Version: ${youds.version}" />
		<echo message="  URL: ${youds.url}" />
		<echo message="" />

		<!-- Print project status. -->
		<echo message="Project:" />
		<youds.locate-project property="project.directory" path="${application.startdir}" ignoreIfSet="true" quiet="true" />
		<youds.check-project property="project.available" path="${project.directory}" />
		<if>
			<isset property="project.available" />
			<then>
				<echo message="  Base directory: ${project.directory}" />
			</then>
			<else>
				<echo message="  (not found)" />
			</else>
		</if>
		<echo message="" />

		<echo message="For a list of possible build targets, call this script with the -l argument." />
	</target>

	<!--
	  System targets.
	  -->

	<target name="system-template-copy" description="Copies a build template to a new location">
		<youds.input property="template.file" message="Template file to copy (relative to build templates directory)" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.available property="template.available" file="templates/${template.file}" type="file" />
		<fail unless="template.available" message="Template file not found in build templates directory" />

		<youds.transform-path property="copy.file.suggest" base="${application.startdir}" path="${template.file}" />

		<youds.input property="copy.file" message="Output file" default="${copy.file.suggest}" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.transform-path property="copy.file" base="${application.startdir}" path="${copy.file}" />

		<youds.directory-name property="copy.directory" path="${copy.file}" />
		<mkdir dir="${copy.directory}" />

		<copy file="templates/${template.file}" tofile="${copy.file}" />
	</target>

	<target name="system-template-copy-all" description="Copies the build template heirarchy to a new location">
		<youds.input property="copy.directory" message="Output directory" default="${application.startdir}" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.transform-path property="copy.directory" base="${application.startdir}" path="${copy.directory}" />

		<mkdir dir="${copy.directory}" />
		<copy todir="${copy.directory}">
			<fileset dir="templates">
				<include name="**/*" />
			</fileset>
		</copy>
	</target>

	<target name="system-source-copy-all" description="Copies the YoudsFramework source tree to a new location">
		<youds.input property="copy.directory" message="Output directory" default="${application.startdir}" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.transform-path property="copy.directory" base="${application.startdir}" path="${copy.directory}" />

		<mkdir dir="${copy.directory}" />
		<copy todir="${copy.directory}">
			<fileset dir="${youds.directory.src}">
				<include name="**/*" />
			</fileset>
		</copy>
	</target>

	<!--
	  Project targets.
	  -->

	<target name="project-locate">
		<youds.locate-project property="project.directory" path="${application.startdir}" ignoreIfSet="true" />
		<youds.input property="project.directory" message="Project base directory" default="${application.startdir}" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.transform-path property="project.directory" base="${application.startdir}" path="${project.directory}" />

		<youds.check-project property="project.available" path="${project.directory}" />
		<fail unless="project.available" message="YoudsFramework project not found in ${project.directory}" />

		<!-- If these properties don't exist, then we assume we're using the old filesystem layout. -->
		<property name="module.action.path.default" value="%core.module_dir%/${property-specifier}{moduleName}/actions/${property-specifier}{actionName}Action.class.php" />
		<property name="module.cache.path.default" value="%core.module_dir%/${property-specifier}{moduleName}/cache/${property-specifier}{actionName}.xml" />
		<property name="module.templates.directory.default" value="%core.module_dir%/${property-specifier}{module}/templates" />
		<property name="module.validate.path.default" value="%core.module_dir%/${property-specifier}{moduleName}/validate/${property-specifier}{actionName}.xml" />
		<property name="module.view.path.default" value="%core.module_dir%/${property-specifier}{moduleName}/views/${property-specifier}{viewName}View.class.php" />
		<property name="module.view.name.default" value="${property-specifier}{actionName}${property-specifier}{viewName}" />

		<!-- If this property doesn't exist, we assume we can use 'build' as the build-system environment. -->
		<property name="project.build.environment" value="build" />

		<!-- Load project-specific build configuration. -->
		<property name="project.file.build-properties" value="${project.directory}/build.properties" />
		<property userProperty="true" file="${project.file.build-properties}" override="true" />

		<!-- Define other module properties. -->
		<property name="module.config.directory" value="config" />
		<property name="module.lib.directory" value="lib" />
		<property name="module.models.directory" value="models" />

		<!-- Define template properties. -->
		<property name="templates.directory" value="${youds.directory.src.build.templates}" />
		<property name="templates.directory.app" value="app" override="true" />
		<property name="templates.directory.app.config" value="${templates.directory.app}/config" override="true" />
		<property name="templates.directory.app.modules" value="${templates.directory.app}/modules" override="true" />
		<property name="templates.directory.app.modules.actions" value="${templates.directory.app.modules}/actions" override="true" />
		<property name="templates.directory.app.modules.views" value="${templates.directory.app.modules}/views" override="true" />
		<property name="templates.directory.app.modules.templates" value="${templates.directory.app.modules}/templates" override="true" />
		<property name="templates.directory.app.modules.config" value="${templates.directory.app.modules}/config" override="true" />
		<property name="templates.directory.app.modules.models" value="${templates.directory.app.modules}/models" override="true" />
		<property name="templates.directory.app.modules.lib" value="${templates.directory.app.modules}/lib" override="true" />
		<property name="templates.directory.app.modules.validate" value="${templates.directory.app.modules}/validate" override="true" />
		<property name="templates.directory.app.modules.cache" value="${templates.directory.app.modules}/cache" override="true" />
		<property name="templates.directory.app.models" value="${templates.directory.app}/models" override="true" />
		<property name="templates.directory.app.lib" value="${templates.directory.app}/lib" override="true" />
		<property name="templates.directory.app.lib.action" value="${templates.directory.app.lib}/action" override="true" />
		<property name="templates.directory.app.lib.view" value="${templates.directory.app.lib}/view" override="true" />
		<property name="templates.directory.app.lib.model" value="${templates.directory.app.lib}/model" override="true" />
		<property name="templates.directory.pub" value="pub" override="true" />
		<property name="templates.directory.templates" value="templates" override="true" />
		<property name="templates.directory.defaults" value="defaults" override="true" />
		<property name="templates.directory.defaults.app" value="${templates.directory.defaults}/app" override="true" />
		<property name="templates.directory.defaults.app.config" value="${templates.directory.defaults.app}/config" override="true" />
		<property name="templates.directory.defaults.app.modules" value="${templates.directory.defaults.app}/modules" override="true" />
		<property name="templates.directory.defaults.app.modules.actions" value="${templates.directory.defaults.app.modules}/actions" override="true" />
		<property name="templates.directory.defaults.app.modules.views" value="${templates.directory.defaults.app.modules}/views" override="true" />
		<property name="templates.directory.defaults.app.modules.templates" value="${templates.directory.defaults.app.modules}/templates" override="true" />
		<property name="templates.directory.defaults.app.templates" value="${templates.directory.defaults.app}/templates" override="true" />
		<property name="templates.directory.defaults.pub" value="${templates.directory.defaults}/pub" override="true" />

		<!-- Define version control properties. -->
		<property name="versioning" value="filesystem" />

		<!-- Ensure we have a working project prefix. -->
		<youds.input property="project.prefix" message="Project prefix" default="Project" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<property userProperty="true" name="project.prefix.input" value="${project.prefix}" override="true" />
		<youds.transform-string-to-identifier property="project.prefix" string="${project.prefix}" />
		<if>
			<not>
				<equals arg1="${project.prefix}" arg2="${project.prefix.input}" />
			</not>
			<then>
				<echo message="Prefix ${project.prefix.input} is not a valid identifier; ${project.prefix} will be used instead" level="warning" />
			</then>
		</if>
	</target>

	<target name="project-wizard-system-action-default" depends="project-locate">
		<youds.car property="module.name" list="${target.project-wizard.modules.names}" delimiter=" ," />
		<youds.input property="module.name" message="Module for default system action" default="${module.name}" promptCharacter=":" failIfEmpty="true" useExistingAsDefault="false" />
		<youds.execute-target name="module-locate" exceptionsFatal="false" />
		<if>
			<not>
				<isset property="module.available" />
			</not>
			<then>
				<youds.execute-target name="module-create" />
			</then>
		</if>

		<youds.input property="action.name" message="Action for default system action" default="Index" promptCharacter=":" failIfEmpty="true" useExistingAsDefault="false" />
		<youds.execute-target name="action-locate" exceptionsFatal="false" />
		<if>
			<not>
				<isset property="action.available" />
			</not>
			<then>
				<youds.execute-target name="action-wizard" />
			</then>
		</if>

		<property userProperty="true" name="system-action.name" value="default" override="true" />
		<youds.execute-target name="project-configuration-set-system-action" />
	</target>

	<target name="project-wizard-welcome-template" depends="project-locate,module-locate,action-locate">
		<property userProperty="true" name="template.name" value="${action.path}Success" override="true" />
		<youds.select-path property="template.path" path="${templates.directory.defaults.app.modules.templates}/WelcomeSuccess.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.resolve-configuration property="template.file" string="${module.view.name}">
			<variable name="actionName" value="${action.path}" />
			<variable name="viewName" value="Success" />
		</youds.resolve-configuration>
		<copy file="${template.path}" tofile="${module.templates.directory}/${template.file}.${template.extension.default}">
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="MODULE_NAME" value="${module.name}" />
					<token key="TEMPLATE_NAME" value="${template.name}" />
					<token key="TEMPLATE_EXTENSION" value="${template.extension.default}" />
				</replacetokens>
			</filterchain>
		</copy>
	</target>

	<target name="project-wizard-welcome" depends="project-locate">
		<!-- Fuck this, man, fuck this. -->

		<property userProperty="true" name="module.name" value="Welcome" override="true" />
		<youds.execute-target name="module-create" />

		<property userProperty="true" name="action.name" value="Index" override="true" />
		<youds.execute-target name="action-create" />

		<property userProperty="true" name="view.name" value="Success" override="true" />
		<youds.select-path property="view.source" path="${templates.directory.defaults.app.modules.views}/WelcomeSuccessView.class.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.execute-target name="view-create" />

		<youds.execute-target name="project-wizard-welcome-template" />
	</target>

	<target name="project-wizard-defaults">
		<!-- Copy special "default" configurations. -->
		<youds.select-path property="template.path" path="${templates.directory.defaults.app.config}">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<copy overwrite="true" todir="${project.directory}/${project.directory.app.config}">
			<mapper type="glob" from="*.xml.tmpl" to="*.xml" />
			<fileset dir="${template.path}">
				<include name="**/*.xml.tmpl" />
			</fileset>
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_NAME" value="${project.name}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="TEMPLATE_EXTENSION" value="${template.extension.default}" />
					<token key="TEMPLATES_LOCATION" value="${templates.directory}" />
				</replacetokens>
			</filterchain>
		</copy>

		<!-- Default global templates. -->
		<youds.select-path property="template.path" path="${templates.directory.defaults.app.templates}">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<copy todir="${project.directory}/${project.directory.app.templates}">
			<mapper type="glob" from="*.php.tmpl" to="*.${template.extension.default}" />
			<fileset dir="${template.path}">
				<include name="**/*.php.tmpl" />
				<exclude name="exceptions/**" />
			</fileset>
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_NAME" value="${project.name}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="TEMPLATE_EXTENSION" value="${template.extension.default}" />
					<token key="TEMPLATES_LOCATION" value="${templates.directory}" />
				</replacetokens>
			</filterchain>
		</copy>
		<copy todir="${project.directory}/${project.directory.app.templates}">
			<mapper type="glob" from="*.php.tmpl" to="*.php" />
			<fileset dir="${template.path}">
				<include name="exceptions/**.php.tmpl" />
			</fileset>
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_NAME" value="${project.name}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="TEMPLATE_EXTENSION" value="${template.extension.default}" />
					<token key="TEMPLATES_LOCATION" value="${templates.directory}" />
				</replacetokens>
			</filterchain>
		</copy>
		
		<!-- stuff from the pub directory -->
		<youds.select-path property="template.path" path="${templates.directory.defaults.pub}">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<copy todir="${project.directory}/${project.directory.pub}">
			<fileset dir="${template.path}">
				<include name="**/*" />
			</fileset>
		</copy>
	</target>

	<target name="project-wizard-system-action-error_404-template" depends="project-locate,module-locate,action-locate">
		<property userProperty="true" name="template.name" value="${action.path}Success" override="true" />
		<youds.select-path property="template.path" path="${templates.directory.defaults.app.modules.templates}/Error404Success.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.resolve-configuration property="template.file" string="${module.view.name}">
			<variable name="actionName" value="${action.path}" />
			<variable name="viewName" value="Success" />
		</youds.resolve-configuration>
		<copy file="${template.path}" tofile="${module.templates.directory}/${template.file}.${template.extension.default}">
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="MODULE_NAME" value="${module.name}" />
					<token key="TEMPLATE_NAME" value="${template.name}" />
					<token key="TEMPLATE_EXTENSION" value="${template.extension.default}" />
				</replacetokens>
			</filterchain>
		</copy>
	</target>

	<target name="project-wizard-system-action-error_404" depends="project-locate">
		<youds.input property="module.name" message="Module for error_404 system action" promptCharacter=":" failIfEmpty="true" />
		<youds.execute-target name="module-locate" />

		<property userProperty="true" name="action.name" value="Error404" override="true" />
		<youds.execute-target name="action-create" />

		<property userProperty="true" name="view.name" value="Success" override="true" />
		<youds.select-path property="view.source" path="${templates.directory.defaults.app.modules.views}/Error404SuccessView.class.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.execute-target name="view-create" />

		<youds.execute-target name="project-wizard-system-action-error_404-template" />

		<property userProperty="true" name="system-action.name" value="error_404" override="true" />
		<youds.execute-target name="project-configuration-set-system-action" />
	</target>

	<target name="project-wizard-system-action-unavailable-template" depends="project-locate,module-locate,action-locate">
		<property userProperty="true" name="template.name" value="${action.path}Success" override="true" />
		<youds.select-path property="template.path" path="${templates.directory.defaults.app.modules.templates}/UnavailableSuccess.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.resolve-configuration property="template.file" string="${module.view.name}">
			<variable name="actionName" value="${action.path}" />
			<variable name="viewName" value="Success" />
		</youds.resolve-configuration>
		<copy file="${template.path}" tofile="${module.templates.directory}/${template.file}.${template.extension.default}">
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="MODULE_NAME" value="${module.name}" />
					<token key="TEMPLATE_NAME" value="${template.name}" />
					<token key="TEMPLATE_EXTENSION" value="${template.extension.default}" />
				</replacetokens>
			</filterchain>
		</copy>
	</target>

	<target name="project-wizard-system-action-unavailable" depends="project-locate">
		<youds.input property="module.name" message="Module for unavailable system action" promptCharacter=":" failIfEmpty="true" />
		<youds.execute-target name="module-locate" />

		<property userProperty="true" name="action.name" value="Unavailable" override="true" />
		<youds.execute-target name="action-create" />

		<property userProperty="true" name="view.name" value="Success" override="true" />
		<youds.select-path property="view.source" path="${templates.directory.defaults.app.modules.views}/UnavailableSuccessView.class.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.execute-target name="view-create" />

		<youds.execute-target name="project-wizard-system-action-unavailable-template" />

		<property userProperty="true" name="system-action.name" value="unavailable" override="true" />
		<youds.execute-target name="project-configuration-set-system-action" />
	</target>

	<target name="project-wizard-system-action-module_disabled-template" depends="project-locate,module-locate,action-locate">
		<property userProperty="true" name="template.name" value="${action.path}Success" override="true" />
		<youds.select-path property="template.path" path="${templates.directory.defaults.app.modules.templates}/ModuleDisabledSuccess.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.resolve-configuration property="template.file" string="${module.view.name}">
			<variable name="actionName" value="${action.path}" />
			<variable name="viewName" value="Success" />
		</youds.resolve-configuration>
		<copy file="${template.path}" tofile="${module.templates.directory}/${template.file}.${template.extension.default}">
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="MODULE_NAME" value="${module.name}" />
					<token key="TEMPLATE_NAME" value="${template.name}" />
					<token key="TEMPLATE_EXTENSION" value="${template.extension.default}" />
				</replacetokens>
			</filterchain>
		</copy>
	</target>

	<target name="project-wizard-system-action-module_disabled" depends="project-locate">
		<youds.input property="module.name" message="Module for module_disabled system action" promptCharacter=":" failIfEmpty="true" />
		<youds.execute-target name="module-locate" />

		<property userProperty="true" name="action.name" value="ModuleDisabled" override="true" />
		<youds.execute-target name="action-create" />

		<property userProperty="true" name="view.name" value="Success" override="true" />
		<youds.select-path property="view.source" path="${templates.directory.defaults.app.modules.views}/ModuleDisabledSuccessView.class.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.execute-target name="view-create" />

		<youds.execute-target name="project-wizard-system-action-module_disabled-template" />

		<property userProperty="true" name="system-action.name" value="module_disabled" override="true" />
		<youds.execute-target name="project-configuration-set-system-action" />
	</target>

	<target name="project-wizard-system-action-secure-template" depends="project-locate,module-locate,action-locate">
		<property userProperty="true" name="template.name" value="${action.path}Success" override="true" />
		<youds.select-path property="template.path" path="${templates.directory.defaults.app.modules.templates}/SecureSuccess.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.resolve-configuration property="template.file" string="${module.view.name}">
			<variable name="actionName" value="${action.path}" />
			<variable name="viewName" value="Success" />
		</youds.resolve-configuration>
		<copy file="${template.path}" tofile="${module.templates.directory}/${template.file}.${template.extension.default}">
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="MODULE_NAME" value="${module.name}" />
					<token key="TEMPLATE_NAME" value="${template.name}" />
					<token key="TEMPLATE_EXTENSION" value="${template.extension.default}" />
				</replacetokens>
			</filterchain>
		</copy>
	</target>

	<target name="project-wizard-system-action-secure" depends="project-locate">
		<youds.input property="module.name" message="Module for secure system action" promptCharacter=":" failIfEmpty="true" />
		<youds.execute-target name="module-locate" />

		<property userProperty="true" name="action.name" value="Secure" override="true" />
		<youds.execute-target name="action-create" />

		<property userProperty="true" name="view.name" value="Success" override="true" />
		<youds.select-path property="view.source" path="${templates.directory.defaults.app.modules.views}/SecureSuccessView.class.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.execute-target name="view-create" />

		<youds.execute-target name="project-wizard-system-action-secure-template" />

		<property userProperty="true" name="system-action.name" value="secure" override="true" />
		<youds.execute-target name="project-configuration-set-system-action" />
	</target>

	<target name="project-wizard-system-action-login" depends="project-locate">
		<youds.input property="module.name" message="Module for login system action" promptCharacter=":" failIfEmpty="true" />
		<youds.execute-target name="module-locate" />

		<property userProperty="true" name="action.name" value="Login" override="true" />
		<youds.select-path property="view.source" path="${templates.directory.defaults.app.modules.actions}/LoginAction.class.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.execute-target name="action-create" />

		<property userProperty="true" name="view.name" value="Success" override="true" />
		<youds.select-path property="view.source" path="${templates.directory.app.modules.views}/View.class.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.execute-target name="view-create" />

		<property userProperty="true" name="view.name" value="Input" override="true" />
		<youds.execute-target name="view-create" />

		<property userProperty="true" name="view.name" value="Error" override="true" />
		<youds.execute-target name="view-create" />

		<property userProperty="true" name="system-action.name" value="login" override="true" />
		<youds.execute-target name="project-configuration-set-system-action" />
	</target>

	<target name="project-wizard" description="Creates a project and corresponding default actions, views, and templates">
		<youds.execute-target name="project-create" />
		<youds.execute-target name="public-create" />

		<youds.execute-target name="project-wizard-defaults" />

		<youds.input property="target.project-wizard.modules.names" message="Space-separated list of modules to create for ${project.name}" default="Default" promptCharacter=":" useExistingAsDefault="false" />
		<youds.iterate property="module.name" target="module-wizard" list="${target.project-wizard.modules.names}" delimiter=" ," exceptionsFatal="false" />

		<!-- System action: default. -->
		<youds.execute-target name="project-wizard-system-action-default" />
		<youds.execute-target name="project-wizard-welcome" />
		<youds.execute-target name="project-wizard-system-action-error_404" />
		<youds.execute-target name="project-wizard-system-action-unavailable" />
		<youds.execute-target name="project-wizard-system-action-module_disabled" />
		<youds.execute-target name="project-wizard-system-action-secure" />
		<youds.execute-target name="project-wizard-system-action-login" />
	</target>

	<target name="project" depends="project-wizard" description="Creates a project and corresponding default actions, views, and templates" />

	<target name="project-create" description="Creates a project">
		<youds.input property="project.directory" message="Project base directory" default="${application.startdir}" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.transform-path property="project.directory" base="${application.startdir}" path="${project.directory}" />

		<youds.check-project property="project.available" path="${project.directory}" />
		<fail if="project.available" message="YoudsFramework project already exists in ${project.directory}" />

		<property userProperty="true" name="project.file.build-properties" value="${project.directory}/build.properties" />
		<property userProperty="true" name="project.file.build-xml" value="${project.directory}/build.xml" />

		<!-- Load project properties (first attempt). -->
		<property userProperty="true" file="${project.file.build-properties}" override="true" />

		<!-- Environment for build system operations. -->
		<youds.input property="project.build.environment" message="YoudsFramework environment for build system operations" default="build" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />

		<!-- Determine project name. -->
		<youds.input property="project.name" message="Project name" default="New YoudsFramework Project" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />

		<!-- Determine project prefix. -->
		<youds.transform-string-to-youds-identifier property="project.prefix.default" string="${project.name}" />
		<youds.input property="project.prefix" message="Project prefix (used, for example, in the project base action)" default="${project.prefix.default}" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<property userProperty="true" name="project.prefix.input" value="${project.prefix}" override="true" />
		<youds.transform-string-to-identifier property="project.prefix" string="${project.prefix}" />
		<if>
			<not>
				<equals arg1="${project.prefix}" arg2="${project.prefix.input}" />
			</not>
			<then>
				<echo message="Prefix ${project.prefix.input} is not a valid identifier; ${project.prefix} will be used instead" level="warning" />
			</then>
		</if>

		<!-- Determine default template extension. -->
		<youds.input property="template.extension.default" message="Default template extension" default="php" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />

		<!-- Determine module paths. -->
		<echo message="YoudsFramework supports customizable paths to various module components. The" />
		<echo message="following variables can be used to specify the paths: ${property-specifier}{moduleName}" />
		<echo message="for the current module name, ${property-specifier}{actionName} for the current action" />
		<echo message="name, and ${property-specifier}{viewName} for the current view name. Note that none of" />
		<echo message="these semantics applies to the templates directory, which only" />
		<echo message="supports the variable ${property-specifier}{module} to represent the current module name." />
		<echo message="For a list of common choices for these settings, see the documentation." />
		<echo message="The defaults are suitable for most projects." />
		<!-- These are the 1.1+ defaults.
		<youds.input property="module.action.path.default" message="Action path" default="%core.module_dir%/${property-specifier}{moduleName}/impl/${property-specifier}{actionName}/Action.class.php" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="module.cache.path.default" message="Cache configuration path" default="%core.module_dir%/${property-specifier}{moduleName}/impl/${actionName}/cache.xml" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="module.validate.path.default" message="Validation configuration path" default="%core.module_dir%/${property-specifier}{moduleName}/impl/${property-specifier}{actionName}/validate.xml" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="module.view.path.default" message="View path" default="%core.module_dir%/${property-specifier}{moduleName}/impl/${property-specifier}{viewName}View.class.php" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="module.view.name.default" message="View name" default="${property-specifier}{actionName}/${property-specifier}{viewName}" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="module.templates.directory.default" message="Templates directory" default="%core.module_dir%/${property-specifier}{module}/impl/" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		-->
		<youds.input property="module.action.path.default" message="Action path" default="%core.module_dir%/${property-specifier}{moduleName}/actions/${property-specifier}{actionName}Action.class.php" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="module.cache.path.default" message="Cache configuration path" default="%core.module_dir%/${property-specifier}{moduleName}/cache/${property-specifier}{actionName}.xml" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="module.validate.path.default" message="Validation configuration path" default="%core.module_dir%/${property-specifier}{moduleName}/validate/${property-specifier}{actionName}.xml" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="module.view.path.default" message="View path" default="%core.module_dir%/${property-specifier}{moduleName}/views/${property-specifier}{viewName}View.class.php" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="module.view.name.default" message="View name" default="${property-specifier}{actionName}${property-specifier}{viewName}" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="module.templates.directory.default" message="Templates directory" default="%core.module_dir%/${property-specifier}{module}/templates" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />

		<!-- Define other module properties. -->
		<property name="module.config.directory" value="config" />
		<property name="module.lib.directory" value="lib" />
		<property name="module.models.directory" value="models" />

		<!-- Define template properties. -->
		<property name="templates.directory" value="${youds.directory.src.build.templates}" />
		<property name="templates.directory.app" value="app" override="true" />
		<property name="templates.directory.app.config" value="${templates.directory.app}/config" override="true" />
		<property name="templates.directory.app.modules" value="${templates.directory.app}/modules" override="true" />
		<property name="templates.directory.app.modules.actions" value="${templates.directory.app.modules}/actions" override="true" />
		<property name="templates.directory.app.modules.views" value="${templates.directory.app.modules}/views" override="true" />
		<property name="templates.directory.app.modules.templates" value="${templates.directory.app.modules}/templates" override="true" />
		<property name="templates.directory.app.modules.config" value="${templates.directory.app.modules}/config" override="true" />
		<property name="templates.directory.app.modules.models" value="${templates.directory.app.modules}/models" override="true" />
		<property name="templates.directory.app.modules.lib" value="${templates.directory.app.modules}/lib" override="true" />
		<property name="templates.directory.app.modules.validate" value="${templates.directory.app.modules}/validate" override="true" />
		<property name="templates.directory.app.modules.cache" value="${templates.directory.app.modules}/cache" override="true" />
		<property name="templates.directory.app.models" value="${templates.directory.app}/models" override="true" />
		<property name="templates.directory.app.lib" value="${templates.directory.app}/lib" override="true" />
		<property name="templates.directory.app.lib.action" value="${templates.directory.app.lib}/action" override="true" />
		<property name="templates.directory.app.lib.view" value="${templates.directory.app.lib}/view" override="true" />
		<property name="templates.directory.app.lib.model" value="${templates.directory.app.lib}/model" override="true" />
		<property name="templates.directory.pub" value="pub" override="true" />
		<property name="templates.directory.templates" value="templates" override="true" />
		<property name="templates.directory.defaults" value="defaults" override="true" />
		<property name="templates.directory.defaults.app" value="${templates.directory.defaults}/app" override="true" />
		<property name="templates.directory.defaults.app.config" value="${templates.directory.defaults.app}/config" override="true" />
		<property name="templates.directory.defaults.app.modules" value="${templates.directory.defaults.app}/modules" override="true" />
		<property name="templates.directory.defaults.app.modules.actions" value="${templates.directory.defaults.app.modules}/actions" override="true" />
		<property name="templates.directory.defaults.app.modules.views" value="${templates.directory.defaults.app.modules}/views" override="true" />
		<property name="templates.directory.defaults.app.modules.templates" value="${templates.directory.defaults.app.modules}/templates" override="true" />
		<property name="templates.directory.defaults.pub" value="${templates.directory.defaults}/pub" override="true" />

		<!-- Create directory structure. -->
		<mkdir dir="${project.directory}" />

		<!-- Create or update build.properties. -->
		<if>
			<equals arg1="${templates.directory}" arg2="${youds.directory.src.build.templates}" />
			<then>
				<property userProperty="true" name="project.file.build-properties.templates-directory" value="${property-specifier}{youds.directory.src.build.templates}" override="true" />
			</then>
			<else>
				<property userProperty="true" name="project.file.build-properties.templates-directory" value="${templates.directory}" />
			</else>
		</if>
		<youds.write-properties file="${project.file.build-properties}" update="true">
			<property name="project.name" value="${project.name}" />
			<property name="project.prefix" value="${project.prefix}" />
			<property name="project.build.environment" value="${project.build.environment}" />
			<property name="template.extension.default" value="${template.extension.default}" />
			<property name="templates.directory" value="${project.file.build-properties.templates-directory}" />
			<property name="module.action.path.default" value="${module.action.path.default}" />
			<property name="module.cache.path.default" value="${module.cache.path.default}" />
			<property name="module.templates.directory.default" value="${module.templates.directory.default}" />
			<property name="module.validate.path.default" value="${module.validate.path.default}" />
			<property name="module.view.path.default" value="${module.view.path.default}" />
			<property name="module.view.name.default" value="${module.view.name.default}" />
		</youds.write-properties>
		<property userProperty="true" file="${project.file.build-properties}" override="true" />

		<!-- Copy a project-specific build.xml if one doesn't already exist. -->
		<if>
			<not>
				<available file="${project.file.build-xml}" type="file" />
			</not>
			<then>
				<youds.select-path property="template.path" path="build.xml.tmpl">
					<from path="${templates.directory}" />
					<from path="${youds.directory.src.build.templates}" />
				</youds.select-path>
				<copy file="${template.path}" tofile="${project.file.build-xml}">
					<filterchain>
						<replacetokens begintoken="%%" endtoken="%%">
							<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
							<token key="PROJECT_LOCATION" value="${project.directory}" />
							<token key="PROJECT_NAME" value="${project.name}" />
							<token key="PROJECT_PREFIX" value="${project.prefix}" />
							<token key="TEMPLATE_EXTENSION" value="${template.extension.default}" />
							<token key="TEMPLATES_LOCATION" value="${templates.directory}" />
						</replacetokens>
					</filterchain>
				</copy>
			</then>
		</if>

		<!-- Create project sub-directories. -->
		<mkdir dir="${project.directory}/${project.directory.app}" />
		<mkdir dir="${project.directory}/${project.directory.app.config}" />
		<mkdir dir="${project.directory}/${project.directory.app.modules}" />
		<mkdir dir="${project.directory}/${project.directory.app.models}" />
		<mkdir dir="${project.directory}/${project.directory.app.cache}" />
		<mkdir dir="${project.directory}/${project.directory.app.lib}" />
		<mkdir dir="${project.directory}/${project.directory.app.log}" />
		<mkdir dir="${project.directory}/${project.directory.app.templates}" />
		<mkdir dir="${project.directory}/${project.directory.dev}" />
		<mkdir dir="${project.directory}/${project.directory.dev.pub}" />
		<mkdir dir="${project.directory}/${project.directory.libs}" />
		<mkdir dir="${project.directory}/${project.directory.pub}" />

		<chmod file="${project.directory}/${project.directory.app.cache}" mode="0777" />
		<chmod file="${project.directory}/${project.directory.app.log}" mode="0777" />

		<!-- Create config.php. -->
		<youds.select-path property="template.path" path="${templates.directory.app}/config.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<copy file="${template.path}" tofile="${project.directory}/${project.directory.app}/config.php">
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_NAME" value="${project.name}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="TEMPLATE_EXTENSION" value="${template.extension.default}" />
					<token key="TEMPLATES_LOCATION" value="${templates.directory}" />
				</replacetokens>
			</filterchain>
		</copy>

		<!-- Create library. -->
		<youds.select-path property="template.path" path="${templates.directory.app.lib}">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<copy todir="${project.directory}/${project.directory.app.lib}">
			<mapper type="regexp" from="^(.+[\\\/])?(.+)\.tmpl$" to="\1/${project.prefix}\2" />
			<fileset dir="${template.path}">
				<include name="**/*.tmpl" />
			</fileset>
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_NAME" value="${project.name}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="TEMPLATE_EXTENSION" value="${template.extension.default}" />
					<token key="TEMPLATES_LOCATION" value="${templates.directory}" />
				</replacetokens>
			</filterchain>
		</copy>

		<!-- Copy configuration. -->
		<youds.select-path property="template.path" path="${templates.directory.app.config}">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<copy todir="${project.directory}/${project.directory.app.config}">
			<mapper type="glob" from="*.xml.tmpl" to="*.xml" />
			<fileset dir="${template.path}">
				<include name="**/*.xml.tmpl" />
			</fileset>
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_NAME" value="${project.name}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="TEMPLATE_EXTENSION" value="${template.extension.default}" />
					<token key="TEMPLATES_LOCATION" value="${templates.directory}" />
				</replacetokens>
			</filterchain>
		</copy>
		
		<!-- Copy the stuff for pub/ to dev/pub, without modifications. -->
		<youds.select-path property="template.path" path="${templates.directory.pub}">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<copy todir="${project.directory}/${project.directory.dev.pub}">
			<mapper type="regexp" from="^(.+[\\\/])?(?:dot(\.))?(.+)\.tmpl$" to="\1/\2\3" />
			<fileset dir="${template.path}">
				<include name="**/*.tmpl" />
			</fileset>
		</copy>
	</target>

	<target name="project-cache-remove" depends="project-locate" description="Removes the contents of a project's filesystem cache">
		<delete failonerror="false" includeemptydirs="true">
			<fileset dir="${project.directory}">
				<include name="${project.directory.app.cache}/**/*" />
			</fileset>
		</delete>
	</target>

	<!--
	  Project configuration targets.
	  -->

	<target name="project-configuration-set-system-action" depends="project-locate" description="Assigns a module and action to a system action">
		<youds.input property="system-action.name" message="System action name" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<if>
			<not>
				<or>
					<equals arg1="${system-action.name}" arg2="default" />
					<equals arg1="${system-action.name}" arg2="error_404" />
					<equals arg1="${system-action.name}" arg2="unavailable" />
					<equals arg1="${system-action.name}" arg2="module_disabled" />
					<equals arg1="${system-action.name}" arg2="secure" />
					<equals arg1="${system-action.name}" arg2="login" />
				</or>
			</not>
			<then>
				<fail message="System action name must be one of {default, error_404, unavailable, module_disabled, secure, login}" />
			</then>
		</if>

		<youds.execute-target name="action-locate" />
		<youds.write-configuration
			namespace="http://youds.com/youds/config/parts/settings/1.1"
			file="${project.directory}/${project.directory.app.config}/settings.xml"
			path="//*[local-name() = 'configuration' and (namespace-uri() = 'http://youds.com/youds/config/global/envelope/1.0' or namespace-uri() = 'http://youds.com/youds/config/global/envelope/1.1')]//*[local-name() = 'system_action' and (namespace-uri() = 'http://youds.com/youds/config/parts/settings/1.0' or namespace-uri() = 'http://youds.com/youds/config/parts/settings/1.1') and @name='${system-action.name}']/*[local-name() = 'module']"
			value="${module.name}"
		/>
		<youds.write-configuration
			namespace="http://youds.com/youds/config/parts/settings/1.1"
			file="${project.directory}/${project.directory.app.config}/settings.xml"
			path="//*[local-name() = 'configuration' and (namespace-uri() = 'http://youds.com/youds/config/global/envelope/1.0' or namespace-uri() = 'http://youds.com/youds/config/global/envelope/1.1')]//*[local-name() = 'system_action' and (namespace-uri() = 'http://youds.com/youds/config/parts/settings/1.0' or namespace-uri() = 'http://youds.com/youds/config/parts/settings/1.1') and @name='${system-action.name}']/*[local-name() = 'action']"
			value="${action.name}"
		/>
	</target>

	<!--
	  Project model targets.
	  -->

	<target name="project-model-locate" depends="project-locate">
		<youds.input property="model.name" message="Model name" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<property userProperty="true" name="model.file" value="${project.directory}/${project.directory.app.models}/${model.name}Model.class.php" />

		<youds.available property="model.available" file="${model.file}" type="file" />
		<fail unless="model.available" message="YoudsFramework model not found at ${model.file}" />

		<!-- Define the name of the view class. -->
		<property userProperty="true" name="model.class" value="${model.name}Model" />
	</target>

	<target name="project-model-create" depends="project-locate" description="Creates a project model">
		<!-- Determine model name. -->
		<youds.input property="model.name" message="Model name" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<property userProperty="true" name="model.name.input" value="${model.name}" override="true" />
		
		<youds.input property="target.model.attributes.names" message="Space separated list of properties for this model" promptCharacter=":" failIfEmpty="false" ignoreIfSet="true" />
		
		<youds.transform-namespace-string-to-identifier property="model.name" string="${model.name}" />
		<if>
			<not>
				<equals arg1="${model.name}" arg2="${model.name.input}" />
			</not>
			<then>
				<echo message="Model name ${model.name.input} is not a valid identifier; ${model.name} will be used instead" level="warning" />
			</then>
		</if>

		<!-- Transform . to _ for the actual identifier. -->
		<youds.transform-string-to-identifier property="model.identifier" string="${model.name}" />

		<youds.transform-namespace-string-to-path property="model.path" string="${model.name}" />
		<property userProperty="true" name="model.file" value="${project.directory}/${project.directory.app.models}/${model.path}Model.class.php" override="true" />

		<youds.available property="model.available" file="${model.file}" type="file" />
		<fail if="model.available" message="Model ${model.name} already exists at ${model.file}" />

		<!-- Define the name of the model class. -->
		<property userProperty="true" name="model.class" value="${model.identifier}Model" override="true" />

		<!-- Generate the code for the attribute list and accessors-->
		<youds.iterate property="model.attribute.name" target="model-attribute-create" list="${target.model.attributes.names}" delimiter=" ," exceptionsFatal="false" />
		<!-- Copy. -->
		<youds.select-path property="template.path" path="${templates.directory.app.models}/Model.class.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<copy file="${template.path}" tofile="${model.file}">
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="MODEL_NAME" value="${model.name}" />
					<token key="MODEL_CLASS" value="${model.class}" />
					<token key="ATTRIBUTE_LIST" value="${model.attributes.list}" /> 
					<token key="ATTRIBUTE_ACCESSORS" value="${model.attributes.accessors}" />
				</replacetokens>
			</filterchain>
		</copy>
	</target>

	<!--
	  Project template targets.
	  -->

	<target name="project-template-locate" depends="project-locate">
		<youds.input property="template.name" message="Template name" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="template.extension" message="Template extension" default="${template.extension.default}" promptCharacter=":" failIfEmpty="true" />
		<property userProperty="true" name="template.file" value="${project.directory}/${project.directory.app.templates}/${template.name}.${template.extension}" override="true" />

		<youds.available property="template.available" file="${template.file}" type="file" />
		<fail unless="template.available" message="YoudsFramework template not found at ${template.file}" />
	</target>

	<target name="project-template-create" depends="project-locate" description="Creates a project template">
		<youds.input property="template.name" message="Template name" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="template.extension" message="Template extension" default="${template.extension.default}" promptCharacter=":" failIfEmpty="true" />
		<property userProperty="true" name="template.file" value="${project.directory}/${project.directory.app.templates}/${template.name}.${template.extension}" override="true" />

		<youds.available property="template.available" file="${template.file}" type="file" />
		<fail if="template.available" message="Template ${template.name}.${template.extension} already exists at ${template.file}" />

		<!-- Copy. -->
		<property userproperty="true" name="template.path.extension" value="" override="true" />
		<youds.select-path property="template.path.extension" path="${templates.directory.templates}/template.${template.extension}.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.select-path property="template.path" path="${templates.directory.templates}/template.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<if>
			<not>
				<equals arg1="${template.path.extension}" arg2="" />
			</not>
			<then>
				<property userProperty="true" name="template.source" value="${template.path.extension}" override="true" />
			</then>
			<else>
				<property userProperty="true" name="template.source" value="${template.path}" override="true" />
			</else>
		</if>
		<copy file="${template.source}" tofile="${template.file}">
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="TEMPLATE_NAME" value="${template.name}" />
					<token key="TEMPLATE_EXTENSION" value="${template.extension}" />
				</replacetokens>
			</filterchain>
		</copy>
	</target>

	<!--
	  Public directory targets.
	  This copies from dev/pub/, mind you!
	  -->

	<target name="public-create" depends="project-locate" description="Creates a structure for the public directory">
		<youds.input property="public.environment" message="Name of the environment to bootstrap in dispatcher scripts" default="development" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		
		<youds.input property="target.public-create.create-htaccess" message="Should an Apache .htaccess file with rewrite rules be generated (y/n)" promptCharacter="?" default="n" failIfEmpty="true" ignoreIfSet="true" />
		<if>
			<or>
				<equals arg1="${target.public-create.create-htaccess}" arg2="yes" trim="true" casesensitive="false" />
				<equals arg1="${target.public-create.create-htaccess}" arg2="y" trim="true" casesensitive="false" />
				<equals arg1="${target.public-create.create-htaccess}" arg2="true" trim="true" casesensitive="false" />
			</or>
			<then>
				<youds.input property="public.base" message="Document-root-relative path to '${project.directory}/${project.directory.pub}/'" default="/" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
				<copy tofile="${project.directory}/${project.directory.pub}/.htaccess" file="${project.directory}/${project.directory.dev.pub}/.htaccess" overwrite="true">
					<filterchain>
						<replacetokens begintoken="%%" endtoken="%%">
							<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
							<token key="PROJECT_LOCATION" value="${project.directory}" />
							<token key="PROJECT_PREFIX" value="${project.prefix}" />
							<token key="PUBLIC_BASE" value="${public.base}" />
							<token key="PUBLIC_ENVIRONMENT" value="${public.environment}" />
						</replacetokens>
					</filterchain>
				</copy>
			</then>
		</if>

		<copy todir="${project.directory}/${project.directory.pub}" overwrite="true">
			<mapper type="regexp" from="^(?!\.htaccess).+" to="\0" />
			<fileset dir="${project.directory}/${project.directory.dev.pub}">
				<include name="**/*" />
			</fileset>
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="PUBLIC_BASE" value="${public.base}" />
					<token key="PUBLIC_ENVIRONMENT" value="${public.environment}" />
				</replacetokens>
			</filterchain>
		</copy>
	</target>

	<!--
	  Module targets.
	  -->

	<target name="module-list" depends="project-locate" description="Displays all modules in a project">
		<youds.display-modules path="${project.directory}" />
	</target>

	<target name="module-locate" depends="project-locate">
		<youds.locate-module property="module.name" path="${application.startdir}" ignoreIfSet="true" />
		<youds.input property="module.name" message="Module name" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<property userProperty="true" name="module.directory" value="${project.directory.app.modules}/${module.name}" override="true" />
		<youds.transform-path property="module.directory" base="${project.directory}" path="${module.directory}" />

		<youds.check-module property="module.available" path="${module.directory}" />
		<fail unless="module.available" message="YoudsFramework module not found in ${module.directory}" />

		<!-- Load module-specific build configuration. -->
		<property userProperty="true" name="module.file.build-properties" value="${module.directory}/build.properties" override="true" />
		<property userProperty="true" file="${module.file.build-properties}" override="true" />

		<!-- Load module configuration. -->
		<youds.configure-module name="${module.name}" prefix="module" />
	</target>

	<target name="module-wizard" depends="project-locate" description="Creates a module and user-specified actions, views, and templates">
		<youds.execute-target name="module-create" />
		
		<youds.input property="target.module-wizard.actions.names" message="Space-separated list of actions to create for ${module.name}" promptCharacter=":" useExistingAsDefault="false" />
		<youds.iterate property="action.name" target="action-wizard" list="${target.module-wizard.actions.names}" delimiter=" ," exceptionsFatal="false" />
	</target>

	<target name="module" depends="module-wizard" description="Creates a module and user-specified actions, views, and templates" />

	<target name="module-create" depends="project-locate" description="Creates a module">
		<!-- Determine module name. -->
		<youds.input property="module.name" message="Module name" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<property userProperty="true" name="module.name.input" value="${module.name}" override="true" />
		<youds.transform-string-to-identifier property="module.name" string="${module.name}" />
		<if>
			<not>
				<equals arg1="${module.name}" arg2="${module.name.input}" />
			</not>
			<then>
				<echo message="Module name ${module.name.input} is not a valid identifier; ${module.name} will be used instead" level="warning" />
			</then>
		</if>

		<property userProperty="true" name="module.directory" value="${project.directory}/${project.directory.app.modules}/${module.name}" override="true" />

		<youds.check-module property="module.available" path="${module.directory}" />
		<fail if="module.available" message="Module ${module.name} already exists at ${module.directory}" />

		<!-- Resolve paths. -->
		<youds.resolve-configuration property="module.templates.directory.resolved" string="${module.templates.directory.default}">
			<variable name="module" value="${module.name}" />
		</youds.resolve-configuration>

		<!-- Create directory structure. -->
		<mkdir dir="${module.directory}" />
		<mkdir dir="${module.directory}/${module.config.directory}" />
		<mkdir dir="${module.directory}/${module.lib.directory}" />
		<mkdir dir="${module.directory}/${module.models.directory}" />
		<mkdir dir="${module.templates.directory.resolved}" />

		<!-- Create library. -->
		<youds.select-path property="template.path" path="${templates.directory.app.modules.lib}">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<copy todir="${module.directory}/${module.lib.directory}">
			<mapper type="regexp" from="^(.+[\\\/])?(.+)\.tmpl$" to="\1/${project.prefix}${module.name}\2" />
			<fileset dir="${template.path}">
				<include name="**/*.tmpl" />
			</fileset>
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="MODULE_NAME" value="${module.name}" />
				</replacetokens>
			</filterchain>
		</copy>

		<!-- Determine the configuration handler paths for actions' XML files. -->
		<youds.resolve-configuration expandDirectives="false" property="module.cache.path.handler" string="${module.cache.path.default}">
			<variable name="moduleName" value="*" />
			<variable name="actionName" value="*" />
		</youds.resolve-configuration>
		<youds.resolve-configuration expandDirectives="false" property="module.validate.path.handler" string="${module.validate.path.default}">
			<variable name="moduleName" value="*" />
			<variable name="actionName" value="*" />
		</youds.resolve-configuration>

		<!-- Copy configuration. -->
		<youds.select-path property="template.path" path="${templates.directory.app.modules.config}">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<copy todir="${module.directory}/${module.config.directory}">
			<mapper type="glob" from="*.xml.tmpl" to="*.xml" />
			<fileset dir="${template.path}">
				<include name="**/*.xml.tmpl" />
			</fileset>
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="MODULE_NAME" value="${module.name}" />
					<token key="MODULE_ACTION_PATH" value="${module.action.path.default}" />
					<token key="MODULE_VIEW_PATH" value="${module.view.path.default}" />
					<token key="MODULE_VIEW_NAME" value="${module.view.name.default}" />
					<token key="MODULE_CACHE_PATH" value="${module.cache.path.default}" />
					<token key="MODULE_VALIDATE_PATH" value="${module.validate.path.default}" />
					<token key="MODULE_TEMPLATES_DIRECTORY" value="${module.templates.directory.default}" />
					<token key="MODULE_CACHE_HANDLER_PATH" value="${module.cache.path.handler}" />
					<token key="MODULE_VALIDATE_HANDLER_PATH" value="${module.validate.path.handler}" />
				</replacetokens>
			</filterchain>
		</copy>
	</target>

	<!--
	  Action targets.
	  -->

	<target name="action-list" depends="project-locate,module-locate" description="Displays all actions in a module">
		<youds.display-actions path="${module.directory}" />
	</target>

	<target name="action-locate" depends="project-locate,module-locate">
		<youds.input property="action.name" message="Action name" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.transform-namespace-string-to-path property="action.path" string="${action.name}" />
		<youds.resolve-configuration property="action.file" string="${module.action.path}">
			<variable name="actionName" value="${action.path}" />
		</youds.resolve-configuration>

		<youds.available property="action.available" file="${action.file}" type="file" />
		<fail unless="action.available" message="YoudsFramework action not found at ${action.file}" />

		<!-- Transform . to _ for the actual identifier. -->
		<youds.transform-string-to-identifier property="action.identifier" string="${action.name}" />

		<!-- Define the name of the action class. -->
		<property userProperty="true" name="action.class" value="${module.name}_${action.identifier}Action" override="true" />
	</target>

	<target name="action-wizard" depends="project-locate,module-locate" description="Creates an action and corresponding views and templates">
		<youds.execute-target name="action-create" />

		<youds.input property="target.action-wizard.views.names" message="Space-separated list of views to create for ${action.name}" default="Success" promptCharacter=":" useExistingAsDefault="false" />
		<youds.iterate property="view.name" target="view-create" list="${target.action-wizard.views.names}" delimiter=" ," exceptionsFatal="false" />

		<property userProperty="true" name="template.extension" value="${template.extension.default}" />
		<youds.iterate property="view.name" target="view-template-create" list="${target.action-wizard.views.names}" delimiter=" ," exceptionsFatal="false" />
	</target>

	<target name="action" depends="action-wizard" description="Creates an action and corresponding views and templates" />

	<target name="action-create" depends="project-locate,module-locate" description="Creates an action">
		<!-- Determine action name. -->
		<youds.input property="action.name" message="Action name" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="action.methods" message="Space-separated list of request methods that should be handled by action [${action.name}] (empty for none)" promptCharacter=":" failIfEmpty="false" ignoreIfSet="false" default="" />
		<youds.input property="action.simple" message="Create a simple action? (y/n)" promptCharacter=":" default="n" failIfEmpty="false" ignoreIfSet="true" />
		
		<property userProperty="true" name="action.name.input" value="${action.name}" override="true" />
		<youds.transform-namespace-string-to-identifier property="action.name" string="${action.name}" />
		<if>
			<not>
				<equals arg1="${action.name}" arg2="${action.name.input}" />
			</not>
			<then>
				<echo message="Action name ${action.name.input} is not a valid identifier; ${action.name} will be used instead" level="warning" />
			</then>
		</if>

		<!-- Transform . to _ for the actual identifier. -->
		<youds.transform-string-to-identifier property="action.identifier" string="${action.name}" />

		<youds.transform-namespace-string-to-path property="action.path" string="${action.name}" />
		<youds.resolve-configuration property="action.file" string="${module.action.path}">
			<variable name="actionName" value="${action.path}" />
		</youds.resolve-configuration>

		<youds.available property="action.available" file="${action.file}" type="file" />
		<fail if="action.available" message="Action ${action.name} already exists at ${action.file}" />

		<!-- Define the name of the action class. -->
		<property userProperty="true" name="action.class" value="${module.name}_${action.identifier}Action" override="true" />
		
		<!-- Copy. -->
		<youds.select-path property="template.path" path="${templates.directory.app.modules.actions}/Action.class.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		
		<youds.select-path property="action.templates.methods.request" path="code/actions/HandleRequestMethod.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.select-path property="action.templates.methods.simple" path="code/actions/Simple.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		
		<property userProperty="true" name="action.methods.declarations" value="" override="true" />
		<youds.generate-action-methods 
			property="action.methods.declarations" 
			methods="${action.methods}" 
			simple="${action.simple}"
			requestMethodTemplate="${action.templates.methods.request}"
			simpleMethodTemplate="${action.templates.methods.simple}"
			/>
		
		<property userProperty="true" name="action.source" value="${template.path}" />
		<copy file="${action.source}" tofile="${action.file}">
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="MODULE_NAME" value="${module.name}" />
					<token key="ACTION_NAME" value="${action.name}" />
					<token key="ACTION_IDENTIFIER" value="${action.identifier}" />
					<token key="ACTION_CLASS" value="${action.class}" />
					<token key="METHOD_DECLARATIONS" value="${action.methods.declarations}" />
				</replacetokens>
			</filterchain>
		</copy>

		<youds.select-path property="template.path" path="${templates.directory.app.modules.validate}/action.xml.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.resolve-configuration property="validate.file" string="${module.validate.path}">
			<variable name="actionName" value="${action.path}" />
		</youds.resolve-configuration>
		<copy file="${template.path}" tofile="${validate.file}">
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="MODULE_NAME" value="${module.name}" />
					<token key="ACTION_NAME" value="${action.name}" />
					<token key="ACTION_IDENTIFIER" value="${action.identifier}" />
					<token key="ACTION_CLASS" value="${action.class}" />
				</replacetokens>
			</filterchain>
		</copy>

		<youds.select-path property="template.path" path="${templates.directory.app.modules.cache}/action.xml.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.resolve-configuration property="cache.file" string="${module.cache.path}">
			<variable name="actionName" value="${action.path}" />
		</youds.resolve-configuration>
		<copy file="${template.path}" tofile="${cache.file}">
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="MODULE_NAME" value="${module.name}" />
					<token key="ACTION_NAME" value="${action.name}" />
					<token key="ACTION_IDENTIFIER" value="${action.identifier}" />
					<token key="ACTION_CLASS" value="${action.class}" />
				</replacetokens>
			</filterchain>
		</copy>
	</target>

	<!--
	  View targets.
	  -->

	<target name="view-locate" depends="project-locate,module-locate,action-locate">
		<youds.input property="view.name" message="View name" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.resolve-configuration property="view.path" string="${module.view.name}">
			<variable name="actionName" value="${action.path}" />
			<variable name="viewName" value="${view.name}" />
		</youds.resolve-configuration>
		<youds.resolve-configuration property="view.file" string="${module.view.path}">
			<variable name="actionName" value="${action.path}" />
			<variable name="viewName" value="${view.path}" />
		</youds.resolve-configuration>

		<youds.available property="view.available" file="${view.file}" type="file" />
		<fail unless="view.available" message="YoudsFramework view not found at ${view.file}" />

		<!-- Define the name of the view class. -->
		<property userProperty="true" name="view.class" value="${module.name}_${action.identifier}${view.name}View" override="true" />
	</target>

	<target name="view-create" depends="project-locate,module-locate,action-locate" description="Creates a view">
		<!-- Determine view name. -->
		<youds.input property="view.name" message="View name" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="view.output-types" message="Space-separated list of output types that should be handled by view ${view.name}" promptCharacter=":" default="html" failIfEmpty="false" ignoreIfSet="false" />
		
		<property userProperty="true" name="view.name.input" value="${view.name}" override="true" />
		<youds.transform-string-to-identifier property="view.name" string="${view.name}" />
		<if>
			<not>
				<equals arg1="${view.name}" arg2="${view.name.input}" />
			</not>
			<then>
				<echo message="View name ${view.name.input} is not a valid identifier; ${view.name} will be used instead" level="warning" />
			</then>
		</if>

		<youds.resolve-configuration property="view.path" string="${module.view.name}">
			<variable name="actionName" value="${action.path}" />
			<variable name="viewName" value="${view.name}" />
		</youds.resolve-configuration>
		<youds.resolve-configuration property="view.file" string="${module.view.path}">
			<variable name="actionName" value="${action.path}" />
			<variable name="viewName" value="${view.path}" />
		</youds.resolve-configuration>

		<youds.available property="view.available" file="${view.file}" type="file" />
		<fail if="view.available" message="View ${view.name} already exists at ${view.file}" />

		<!-- Define the name of the view class. -->
		<youds.resolve-configuration property="view.class.base" string="${module.view.name}">
			<variable name="actionName" value="${action.identifier}" />
			<variable name="viewName" value="${view.name}" />
		</youds.resolve-configuration>
		<youds.transform-view-class-base property="view.class.base" string="${view.class.base}" />
		<property userProperty="true" name="view.class" value="${module.name}_${view.class.base}View" override="true" />
		
		<property userProperty="true" name="view.method-declarations" value="" override="true" />
		<!-- generate the output-type handling methods -->
		<youds.iterate property="view.output-type.name" target="view-methods-create" list="${view.output-types}" delimiter=" ," exceptionsFatal="false" />
			
		<!-- Copy. -->
		<youds.select-path property="template.path" path="${templates.directory.app.modules.views}/View.class.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<property userProperty="true" name="view.source" value="${template.path}" />
		<copy file="${view.source}" tofile="${view.file}">
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="MODULE_NAME" value="${module.name}" />
					<token key="ACTION_NAME" value="${action.name}" />
					<token key="ACTION_IDENTIFIER" value="${action.identifier}" />
					<token key="ACTION_CLASS" value="${action.class}" />
					<token key="VIEW_NAME" value="${view.name}" />
					<token key="VIEW_CLASS" value="${view.class}" />
					<token key="METHOD_DECLARATIONS" value="${view.method-declarations}" />
				</replacetokens>
			</filterchain>
		</copy>
	</target>

	<target name="view-methods-create">
		<!-- generate the output-type handling methods -->
		<youds.select-path property="view.templates.methods.output-type">
			<from path="${templates.directory}/code/views/HandleOutputType.${view.output-type.name}.tmpl" />
			<from path="${templates.directory}/code/views/HandleOutputType.tmpl" />
			<from path="${youds.directory.src.build.templates}/code/views/HandleOutputType.${view.output-type.name}.tmpl" />	
			<from path="${youds.directory.src.build.templates}/code/views/HandleOutputType.tmpl" />
		</youds.select-path>
		<youds.generate-view-methods 
			property="view.method-declarations" 
			outputType="${view.output-type.name}" 
			actionName="${action.name}"
			methodTemplate="${view.templates.methods.output-type}"
			/>
	</target>
	
	<target name="view-template-create" depends="project-locate,module-locate,action-locate,view-locate" description="Creates a template for a view">
		<youds.resolve-configuration property="template.name" string="${module.view.name}">
			<variable name="actionName" value="${action.path}" />
			<variable name="viewName" value="${view.name}" />
		</youds.resolve-configuration>
		<youds.execute-target name="template-create" />
	</target>

	<!--
	  Model targets.
	  -->

	<target name="model-locate" depends="project-locate,module-locate">
		<youds.input property="model.name" message="Model name" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<property userProperty="true" name="model.file" value="${module.directory}/${module.models.directory}/${model.name}Model.class.php" />

		<youds.available property="model.available" file="${model.file}" type="file" />
		<fail unless="model.available" message="YoudsFramework model not found at ${model.file}" />

		<!-- Define the name of the view class. -->
		<property userProperty="true" name="model.class" value="${module.name}_${model.name}Model" />
	</target>

	<target name="model-create" depends="project-locate,module-locate" description="Creates a model">
		<!-- Determine model name. -->
		<youds.input property="model.name" message="Model name" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="target.model.attributes.names" message="Space separated list of properties for this model" promptCharacter=":" failIfEmpty="false" ignoreIfSet="true" />
		<property userProperty="true" name="model.name.input" value="${model.name}" override="true" />
		<youds.transform-namespace-string-to-identifier property="model.name" string="${model.name}" />
		<if>
			<not>
				<equals arg1="${model.name}" arg2="${model.name.input}" />
			</not>
			<then>
				<echo message="Model name ${model.name.input} is not a valid identifier; ${model.name} will be used instead" level="warning" />
			</then>
		</if>

		<!-- Transform . to _ for the actual identifier. -->
		<youds.transform-string-to-identifier property="model.identifier" string="${model.name}" />

		<youds.transform-namespace-string-to-path property="model.path" string="${model.name}" />
		<property userProperty="true" name="model.file" value="${module.directory}/${module.models.directory}/${model.path}Model.class.php" override="true" />

		<youds.available property="model.available" file="${model.file}" type="file" />
		<fail if="model.available" message="Model ${model.name} already exists at ${model.file}" />

		<!-- Define the name of the model class. -->
		<property userProperty="true" name="model.class" value="${module.name}_${model.identifier}Model" override="true" />

		<!-- Generate the code for the attribute list and accessors-->
		
		<property userProperty="true" name="model.attributes.list" value="" override="true" />
		<property userProperty="true" name="model.attributes.accessors" value="" override="true" />
		<youds.iterate property="model.attribute.name" target="model-attribute-create" list="${target.model.attributes.names}" delimiter=" ," exceptionsFatal="false" />
		
		<!-- Copy. -->
		<youds.select-path property="template.path" path="${templates.directory.app.modules.models}/Model.class.php.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<copy file="${template.path}" tofile="${model.file}">
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="MODULE_NAME" value="${module.name}" />
					<token key="MODEL_NAME" value="${model.name}" />
					<token key="MODEL_CLASS" value="${model.class}" />
					<token key="ATTRIBUTE_LIST" value="${model.attributes.list}" /> 
					<token key="ATTRIBUTE_ACCESSORS" value="${model.attributes.accessors}" />
				</replacetokens>
			</filterchain>
		</copy>
	</target>
	
	<target name="model-attribute-create" description="Creates the attributes for a model">
		<youds.input property="model.attribute.type" message="Type for attribute ${model.attribute.name}" default="mixed" promptCharacter=":" failIfEmpty="true" ignoreIfSet="false" />
		<youds.input property="model.attribute.access.level" message="The access for attribute ${model.attribute.name} (public, protected, private)" default="protected" promptCharacter=":" failIfEmpty="true" ignoreIfSet="false" />
		
		<youds.select-path property="model.templates.getter.path" path="code/models/AttributeGetter.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.select-path property="model.templates.setter.path" path="code/models/AttributeSetter.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.select-path property="model.templates.attribute.path" path="code/models/Attribute.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		
		<youds.generate-model-attributes 
			attributeListProperty="model.attributes.list" 
			attributeAccessorsProperty="model.attributes.accessors" 
			attributeName="${model.attribute.name}" 
			attributeType="${model.attribute.type}" 
			attributeAccessLevel="${model.attribute.access.level}" 
			attributeTemplate="${model.templates.attribute.path}"
			attributeSetterTemplate="${model.templates.setter.path}"
			attributeGetterTemplate="${model.templates.getter.path}"
			/>
		
	</target>
	<!--
	  Template targets.
	  -->

	<target name="template-locate" depends="project-locate,module-locate">
		<youds.input property="template.name" message="Template name" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="template.extension" message="Template extension" default="${template.extension.default}" promptCharacter=":" failIfEmpty="true" />
		<property userProperty="true" name="template.file" value="${module.templates.directory}/${template.name}.${template.extension}" override="true" />

		<youds.available property="template.available" file="${template.file}" type="file" />
		<fail unless="template.available" message="YoudsFramework template not found at ${template.file}" />
	</target>

	<target name="template-create" depends="project-locate,module-locate" description="Creates a template">
		<youds.input property="template.name" message="Template name" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<youds.input property="template.extension" message="Template extension" default="${template.extension.default}" promptCharacter=":" failIfEmpty="true" ignoreIfSet="true" />
		<property userProperty="true" name="template.file" value="${module.templates.directory}/${template.name}.${template.extension}" override="true" />

		<youds.available property="template.available" file="${template.file}" type="file" />
		<fail if="template.available" message="Template ${template.name}.${template.extension} already exists at ${template.file}" />

		<!-- Copy. -->
		<property userproperty="true" name="template.path.extension" value="" override="true" />
		<youds.select-path property="template.path.extension" path="${templates.directory.templates}/template.${template.extension}.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<youds.select-path property="template.path" path="${templates.directory.templates}/template.tmpl">
			<from path="${templates.directory}" />
			<from path="${youds.directory.src.build.templates}" />
		</youds.select-path>
		<if>
			<not>
				<equals arg1="${template.path.extension}" arg2="" />
			</not>
			<then>
				<property userProperty="true" name="template.source" value="${template.path.extension}" override="true" />
			</then>
			<else>
				<property userProperty="true" name="template.source" value="${template.path}" override="true" />
			</else>
		</if>
		<copy file="${template.source}" tofile="${template.file}">
			<filterchain>
				<replacetokens begintoken="%%" endtoken="%%">
					<token key="AGAVI_SOURCE_LOCATION" value="${youds.directory.src}" />
					<token key="PROJECT_LOCATION" value="${project.directory}" />
					<token key="PROJECT_PREFIX" value="${project.prefix}" />
					<token key="MODULE_NAME" value="${module.name}" />
					<token key="TEMPLATE_NAME" value="${template.name}" />
					<token key="TEMPLATE_EXTENSION" value="${template.extension}" />
				</replacetokens>
			</filterchain>
		</copy>
	</target>

	<!-- Post-initialization. -->
	<if>
		<isset property="project.directory" />
		<then>
			<property userProperty="true" file="${project.directory}/build.properties" />
			<youds.import file="${project.directory}/build.xml" />
		</then>
	</if>

</project>
