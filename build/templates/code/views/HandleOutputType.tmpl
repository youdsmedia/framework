

	/**
	 * Handles the %%OUTPUT_TYPE_NAME%% output type.
	 *
	 * @parameter  YoudsFrameworkRequestDataHolder the (validated) request data
	 *
	 * @return     mixed <ul>
	 *                     <li>An YoudsFrameworkExecutionContainer to forward the execution to or</li>
	 *                     <li>Any other type will be set as the response content.</li>
	 *                   </ul>
	 */
	public function execute%%OUTPUT_TYPE_NAME%%(YoudsFrameworkRequestDataHolder $rd)
	{
		$this->setup%%OUTPUT_TYPE_NAME%%($rd);

		$this->setAttribute('_title', '%%ACTION_NAME%%');
	}
